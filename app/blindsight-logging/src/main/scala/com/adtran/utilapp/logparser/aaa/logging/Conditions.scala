package com.adtran.utilapp.logparser.aaa.logging


import io.circe.Json

object Conditions {

//  val dev: Condition    = Condition.always
//  val review: Condition = Condition.diagnostic
//  val live: Condition   = Condition.operational
//
//  // Turns on low-level debugging for Cats Parsing things will ParseResult or something.
//  val parsingDebug = new Condition:
//    override def test(level: Level, context: LoggingContext): Boolean = {
//      println(context.loggerFields.toString())
//      true
//    }
}
